export enum SourceType {
    // Geojson,
    // Gpx,
    Points = "Point",
    Lines = "LineString",
    Polygons = "Polygon",
}

export enum LayerType {
    Point = "Circle",
    Line = "Line",
    Polygon = "Polygon",
    Photo = "Photo",
    Heatmap = "Heatmap",
}
